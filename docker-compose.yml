version: "3.9"

services:
  auth:
    image: neha544/pms-auth-auth:latest
    container_name: auth
    ports:
      - "8081:8081"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - pms-network

networks:
  pms-network:
    external: true
